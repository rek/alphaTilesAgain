# data-players

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-players` to build the library.

## Running unit tests

Run `nx test data-players` to execute the unit tests via [Jest](https://jestjs.io).
