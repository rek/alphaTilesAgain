# util-phoneme

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-phoneme` to build the library.

## Running unit tests

Run `nx test util-phoneme` to execute the unit tests via [Jest](https://jestjs.io).
