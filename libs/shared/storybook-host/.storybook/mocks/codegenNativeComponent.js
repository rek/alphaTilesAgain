/**
 * Stub for react-native/Libraries/Utilities/codegenNativeComponent.
 * This module is native-only and cannot run in Vite/browser context.
 * react-native-safe-area-context uses it in its NativeSafeAreaView spec.
 * We stub it so Storybook can import SafeAreaProvider without crashing.
 */
export default function codegenNativeComponent() {
  return null;
}
