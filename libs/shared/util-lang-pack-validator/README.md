# util-lang-pack-validator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-lang-pack-validator` to build the library.

## Running unit tests

Run `nx test util-lang-pack-validator` to execute the unit tests via [Jest](https://jestjs.io).
