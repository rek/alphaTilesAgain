import { utilLangPackValidator } from './util-lang-pack-validator';

describe('utilLangPackValidator', () => {
  it('should work', () => {
    expect(utilLangPackValidator()).toEqual('util-lang-pack-validator');
  });
});
