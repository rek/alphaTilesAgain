/** Mock for @shared/util-analytics in unit tests. */
export const track = jest.fn();
export const identify = jest.fn();
export const screen = jest.fn();
export const useTrackScreenMount = jest.fn();
