# data-audio

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-audio` to build the library.

## Running unit tests

Run `nx test data-audio` to execute the unit tests via [Jest](https://jestjs.io).
